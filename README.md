# produccion
 Qa
 develop
 back
 front

 git pull origin nombre-rama // trae y actualiza la rama
 git branch // crea ramas
 git status // mueestra de manera local los cambios realizados
 git add . // añade los cambios de manera local
 git commit -m "mensaje" // añade los cambios a la rama local
 git push origin nombre-rama // sube los cambios a la rama remota


comentarios "create" "feat / " "delete" "config"
porfis //visita a isa hermosa 

